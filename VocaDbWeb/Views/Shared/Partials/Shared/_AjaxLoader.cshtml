@using VocaDb.Web.Models.Shared.Partials.Shared
@model AjaxLoaderViewModel

@if (Model.Id != null)
{
	<img id="@Model.Id" src="@Url.Content("~/Content/ajax-loader.gif")" alt="loading..." style="display:none;" />
}
else
{
	<img src="@Url.Content("~/Content/ajax-loader.gif")" alt="loading..." />
}