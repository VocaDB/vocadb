@using VocaDb.Web.Models.Shared.Partials.User;
@model UserIconLinkOrNameViewModel

@if (Model.User != null) {
	@UserHelpers.UserIconLink_IUserWithEmail(Model.User, Model.Size)
} else {
	@Model.Name
}