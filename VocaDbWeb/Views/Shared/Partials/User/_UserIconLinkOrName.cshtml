@using VocaDb.Web.Models.Shared.Partials.User;
@model UserIconLinkOrNameViewModel

@if (Model.User != null) {
	@Html.Partial("Partials/User/_UserIconLink_IUserWithEmail", new UserIconLink_IUserWithEmailViewModel(Model.User, Model.Size))
} else {
	@Model.Name
}