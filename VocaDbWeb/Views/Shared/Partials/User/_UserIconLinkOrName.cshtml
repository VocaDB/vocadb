@using VocaDb.Web.Models.Shared.Partials.User
@model UserIconLinkOrNameViewModel

@if (Model.User != null) {
	<partial name="Partials/User/_UserIconLink_IUserWithEmail" model="@(new UserIconLink_IUserWithEmailViewModel(Model.User, Model.Size))" />
} else {
	@Model.Name
}