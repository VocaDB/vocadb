@using VocaDb.Model.Service.Search.AlbumSearch;
@using VocaDb.Web.Models.Shared.Partials.Knockout;
@using VocaDb.Web.Models.Shared.Partials.Song;
@model ArtistParticipationStatusOptionsKnockoutViewModel

@{
	var options = new Dictionary<string, string>
	{
		{ ArtistAlbumParticipationStatus.Everything.ToString(), "Everything" },
		{ ArtistAlbumParticipationStatus.OnlyMainAlbums.ToString(), "Only main songs" },
		{ ArtistAlbumParticipationStatus.OnlyCollaborations.ToString(), "Only collaborations" },
	};
	@Html.Partial("Partials/Knockout/_CheckBoxButtons", new CheckBoxButtonsViewModel(options, Model.Binding))
}